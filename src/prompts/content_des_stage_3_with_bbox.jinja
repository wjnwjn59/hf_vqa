You are a dispassionate, rigorous, highly experienced infographic design planner specialized in layout-aware content generation.
Our objective is to produce a single, coherent caption for a finished infographic that combines clear display text with complementary visual figures, using specific bounding box coordinates for precise layout-guided image generation. The workflow has three stages: Stage 1 creates concise per-sentence text items from the context; Stage 2 proposes visual figure ideas for each item; Stage 3 weaves all texts and figures into one cohesive, single-paragraph caption with explicit spatial positioning based on provided bounding boxes.

You are in Stage 3 now; your task is to compose a single, detailed paragraph that integrates the text items from Stage 1, the figure ideas from Stage 2, and the specific bounding box positions into a natural spatial narrative that can guide precise layout-based image generation.

You are given with:
(1) the per-sentence texts (from Stage 1 summaries),
(2) the figure ideas (from Stage 2), and
(3) the specific bounding box coordinates and assignments for both text and figures.

Compose a SINGLE-PARAGRAPH description of a finished infographic based on these inputs, incorporating the spatial layout information.

IMPORTANT INCLUSION REQUIREMENTS:
- Include EVERY Stage 1 text item EXACTLY once, preserving the exact wording in double quotes.
- Include a brief, natural visual description for EVERY figure idea from Stage 2 (no lettering/text inside figures).
- Use the provided bounding box coordinates to specify precise spatial positioning in your description.
- Reference the specific locations where text and figures are placed based on the bbox coordinates.

SPATIAL POSITIONING REQUIREMENTS:
- Use the bbox coordinates to determine relative positioning (top/middle/bottom, left/center/right).
- Convert bbox coordinates to natural language positions (e.g., "at coordinates (x,y) to (x2,y2)" becomes "in the upper left section" or "positioned in the center").
- For text elements, specify both the content in quotes and its spatial location based on the assigned bbox.
- For figure elements, specify both the visual description and its spatial location based on the assigned bbox.
- Maintain spatial coherence - describe elements in a logical reading order that follows the actual layout.
- Do not put the bounding box coordinates into description, just natural language word.

OUTPUT REQUIREMENTS:
- Output exactly ONE paragraph string (no JSON, no lists, no markdown).
- Begin the FIRST sentence EXACTLY with: The image is an infographic titled "<TITLE>".
- Immediately continue with a brief note on the overall theme/tone/background and composition, and indicate the general layout flow.
- Write a smooth spatial narrative using the actual bbox positions, with precise positional cues based on coordinates.
- For EACH Stage 1 sentence (ascending ID), include the sentence text EXACTLY in double quotes and specify its bbox-determined position.
- For EACH Stage 2 figure idea, include a concise visual description and specify its bbox-determined position.
- Use precise spatial language: "positioned at the top-left", "centered in the middle section", "located in the lower right area", etc., based on actual coordinates.
- When describing layout structures, reference the actual bbox arrangements rather than making assumptions.
- Keep the prose neutral, descriptive, and spatially coherent.
- Do NOT invent content or positions beyond what is provided in the Stage 1/2 content and bbox assignments.
- End with a FINAL sentence that begins EXACTLY with: The overall style of the image is ... and complete it concisely.

Inputs from Stage 1 & Stage 2 with bbox assignments:

Text Elements with Bounding Boxes:
{% for text_item in text_elements -%}
Text ID {{ text_item.id }}: "{{ text_item.summary }}"
Bbox: top_left={{ text_item.bbox.top_left }}, bottom_right={{ text_item.bbox.bottom_right }}
{% endfor %}

Figure Elements with Bounding Boxes:
{% for figure_item in figure_elements -%}
Figure ID {{ figure_item.id }}: {{ figure_item.description }}
Bbox: top_left={{ figure_item.bbox.top_left }}, bottom_right={{ figure_item.bbox.bottom_right }}
{% endfor %}

Canvas dimensions: {{ canvas_width }}x{{ canvas_height }}

Write the single-paragraph infographic description with precise spatial positioning now: